## Summary

<!-- What does this PR do, and why? -->

## Checklist

- [ ] Tests added/updated for the change (`pnpm test`)
- [ ] `pnpm run typecheck`, `pnpm run lint`, `pnpm run format:check` pass
- [ ] `dist/` was rebuilt (`pnpm run build`) if `src/**` changed, and
      `check-dist` is green
- [ ] `README.md`'s input/output table was updated to match `action.yml`,
      if inputs/outputs changed
- [ ] `docs/manifests.md` and/or fixtures were updated, if manifest
      handling changed
- [ ] Commit messages follow Conventional Commits
