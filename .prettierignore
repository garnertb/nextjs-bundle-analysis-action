dist/
lib/
pnpm-lock.yaml

# Real `next build` output committed verbatim as test fixtures; must not be
# reformatted or its bytes no longer match a real build.
fixtures/next/

# Generated by release-please; its bullet/spacing style doesn't match
# Prettier's, and it must never be hand-formatted anyway.
CHANGELOG.md
